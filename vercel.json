{
  "buildCommand": "yarn build",
  "outputDirectory": ".output",
  "installCommand": "yarn install",
  "devCommand": "yarn dev",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
